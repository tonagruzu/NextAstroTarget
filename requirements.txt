# Core dependencies for NextAstroTarget application
pandas>=2.0.0
openpyxl>=3.1.0
# sqlite3 is built-in with Python - no installation needed
PySide6>=6.5.0  # Modern Qt6 GUI framework (replaces tkinter)
Pillow>=9.0.0
requests>=2.28.0

# Optional astronomical libraries
astropy>=5.0.0
ephem>=4.1.0

# Development and testing (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0